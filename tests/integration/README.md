# Integration tests

Integration tests will exercise boundaries between the EPUB adapter, renderer,
PDF output, and validator after those components have implementations. Keep
small, redistributable test publications under `tests/fixtures` and document
their source and license.
