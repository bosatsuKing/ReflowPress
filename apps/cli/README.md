# CLI application

This workspace location is reserved for the future command-line interface. No
commands are implemented in the foundation phase. The CLI will call the shared
Core contracts instead of owning EPUB parsing or PDF rendering logic.
